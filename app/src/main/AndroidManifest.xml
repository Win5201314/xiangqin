<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zsl.itgod">

    <!-- 网络相关 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--允许读取手机状态 用于创建BmobInstallation-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="com.zsl.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/root"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/root"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:theme="@style/newMDTheme"
            android:name=".ContactMeActivity"/>
        <activity android:theme="@style/newMDTheme"
            android:name=".PutJobActivity"/>
        <activity android:theme="@style/newMDTheme"
            android:name=".ShowActivity"/>
        <activity android:theme="@style/newMDTheme"
            android:name=".JobActivity"/>
    </application>

</manifest>